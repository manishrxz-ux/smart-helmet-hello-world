server.port=${PORT:8080}

# Spring Data JPA & MySQL Shared Hosting Database Connection Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://37.27.71.198:3306/uvmtgikg_smarthelmet?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:uvmtgikg_smartuser}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:SmartHelmet2027!}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.MySQLDialect}
